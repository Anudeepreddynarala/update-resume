{
  "tools": [],
  "mcpServers": {
    "resume-optimizer-mcp": {
      "command": "uv",
      "args": [
        "run",
        "--with","mcp[cli]",
        "mcp",
        "run",
        "mcp_server.py"
      ],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
